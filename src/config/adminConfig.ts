// Encrypted admin credentials
// The password is encrypted and must be decrypted using the encryption utility
// Default password: briiite2025 (encrypted)

export const ENCRYPTED_ADMIN_PASSWORD = 'AAAAAAAAAG1dW1E=';

// To change the password:
// 1. Use the encryptPassword function from utils/encryption.ts
// 2. Run: encryptPassword('your_new_password')
// 3. Update ENCRYPTED_ADMIN_PASSWORD with the result
